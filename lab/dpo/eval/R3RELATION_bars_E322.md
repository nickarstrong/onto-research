# R3RELATION_bars_E322 -- relation-anchor guard verdict (v322 TYPE-C)

Plane: v322 TYPE-C build of the OWED-R3RELATION relation-anchor precondition on the Layer-1
hermetic structural discriminator. Additive, monotone (ABSTAIN-only; never a new DIRTY). The
frozen two-year/span subtraction check is unchanged; it runs only when an in-claim duration
relation binds.

Mechanism: a relation-anchor eligibility predicate gates the existing subtraction. Eligibility =
both in-claim years and the span value co-bind ONE single-sentence window under a closed
duration-cue set (hermetic; no oracle, no network). Not bound -> ABSTAIN (honest floor).

Pre-registered bars (falsifier-first, fixed before measurement):
- B0-strict35 : 0 false-DIRTY on the canonical 35 GT-CLEAN (isolated).         RESULT 0/35   -> GREEN
- B-recall    : all 5 internal-contradiction rows retained DIRTY.              RESULT 5/5    -> GREEN
- B-relation  : the documented spurious-subtraction clean fixture -> ABSTAIN.  RESULT ABSTAIN -> GREEN
- B-hermetic  : two cold offline runs byte-identical.                          RESULT match  -> GREEN

Verdict: ALL 4 GREEN. OWED-R3RELATION CLOSED. The internal-contradiction channel is certified
strict-B0-clean on the canonical 35 (isolated for the first time). Channel coverage is unchanged
(the (C) reframe stays terminal, v320). Harness, held-out fixtures, and the derived B0 set stay
LOCAL (KEEP-LOCAL); this report carries counts + mechanism only.
