# lab/dpo/.gitignore -- keep held-out / bait / runtime OUT of public git
# (pack v244 sec 3.0 DO-FIRST landmine: one `git add .` publishes these and
#  kills the measurement -- §3.2). Public git carries ONLY datable provenance +
#  reproducibility (data + scripts + gate.log + reports).

# held-out eval (poisons measurement if it enters a future pretrain)
o0_heldout_gen.jsonl
o0_heldout*.jsonl

# bait sets + manual bait verdicts
*bait*

# runtime tier state / curated view (rebuilt each run; NOT provenance -- pack sec 1)
o0_tier_state.json
o0_verdicts_curated.jsonl

# backups / caches / logs / local data
*.bak.*
__pycache__/
logs/
data/
eval/o0/v282_retrieve_audit*.jsonl
eval/o0/step6_falsifier_E*.json
reports/PREREG_*.md

# === WATCH-D KEEP-LOCAL (v286) : persistent local artifacts, never public ===
claims_*
verdicts_B*.jsonl
auditor_*
audit_*
contact_*
*.log
RUN.txt
proposals_v01b.jsonl
raw_v01b.jsonl
v3_blind_slice.jsonl
o0_tombstones.jsonl
reharvest_*
bq_rescore_fixture.jsonl
gold_frame.txt
selflearn_trace.*
leverage_out/
eval/o0/archive/
eval/o0/o0_recut_candidates.jsonl
eval/o0/o0_s4_gate_v2_trace.jsonl
eval/o0/o0_subclaim_probe.jsonl
eval/ordinary_window_v6_clean.jsonl
eval/ordinary_window_v6_dpo_clean.jsonl
eval/ordinary_window_v7_clean.jsonl
eval/ordinary_window_v7_dpo_clean.jsonl
