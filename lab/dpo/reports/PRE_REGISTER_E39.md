# PRE_REGISTER_E39.md -- v4 (noauth-split) -- cross-source statistic D_lambda over the AUTH subset

status      : FROZEN (pre-data). Supersedes v3 (md5 04b83e395a5c7f2cf9c0e29aba9bbfdc, RETIRED -- contract
              defect: sec6 required D_0==E37-label on GOLDS, contradicting the lift premise; reports/E39_V3_DEFECT.md).
              v3<-v2 (shape-gate, E39_V2_DEFECT.md) <-v1 (a78919237ca2335a945b968e971b418f, E39_RECONCILE_DEFECT.md).
date        : 2026-06-11
rung        : E39 (after E37 SET_EXHAUSTED 88489f8 ; E38 B_STATISTIC_WRONG 90b9f46 branch-A FALSIFIED).
question    : does a net-consensus statistic D_lambda = (n_con - lambda*n_ent)/|S| -- which reads the entailment
              asymmetry that con_share discards -- LIFT the bf-band on the auth subset, where con/ent actually vary?
load class  : MODEL. The readout is REGENERATED by emit_E37_readout.py replaying the FROZEN E37 op-point over
              heldout_E17 (DeBERTa NLI: ce_scores + ce_entail). E37 never serialized con/ent (report_E37.md only);
              eval/_local/E37_boundset.json was a retrieval map -> OVERWRITTEN by the 6-field readout.

## sec0 SUBSTRATE (md5-gated; LOCAL/private; NEVER public -- pretrain-eat)
- scorer source   : run_E37_probe.py md5 15e694a6690a70b801431d64c0b5c368 (commit 88489f8) -- IMPORTED, UNCHANGED.
- heldout         : eval/_local/heldout_E17.jsonl   md5 7e9fe030646d5671952e7a9fe9437e37
- fixture         : eval/_local/gold_fixture_E25b.json md5 4a45f52883a802e8d8d1d5ff5d185bdb
- NLI model       : MoritzLaurer/DeBERTa-v3-large-mnli-fever-anli-ling-wanli (inherited; con/ent idx from id2label).
- retrieve floor  : ONTO_RETRIEVE_FLOOR=0.45 (set before semantic_retrieve import; byte-identical to E37 main).

## sec1 FROZEN E37 OP-POINT (replayed, NOT re-selected; recorded gate_E37.log)
C = 0.0012960433959960938 ; A = 0.35 ; B_floor = 0.1 ; scope = "any" ; W = 0.0 ; P = 0.67 (= tau_E37).
The emitter does NOT call select_op. It replays these exact values; the anchor (sec4) validates the op-point.

## sec2 READOUT SCHEMA (per B1/B2 item ; the ONLY 6 fields ; LOUD-FAIL on any other shape)
record = { item_id: str, n_con: int, n_ent: int, S_size: int, true_class: "gold"|"spoof", pre_demoted: bool }
- true_class : "spoof" iff class == B1_CLASS ("spoof_cuestripped_entitied") ; "gold" iff class == B2_CLASS
               ("gold_backed") and id not in B2_EXCLUDE ({"ho_23"}). B3 (negctrl_common) is NOT emitted.
- pre_demoted: TRUE iff item_label_at_C(res, C=+inf, B_floor=0.0, scope="any") == "DEMOTE". This is the
               unconditional (noauth) channel -- model-independent, BYTE-IDENTICAL to s3_attribution's `base`.
               For pre_demoted rows S MAY be empty; D_lambda is SKIPPED (no fabricated con -- R7).
- REPRESENTATIVE CLAIM (non-pre_demoted, defines n_con/n_ent/S_size): over the item's auth resolutions, at the
               FROZEN op-point compute S=_bound_subset(cands, B_floor=0.1, "any", W=0), n_con=#{con>=C},
               n_ent=#{ent>=A}, con_share=n_con/|S|. SELECT the claim with MAX con_share (tie-break: MIN n_ent).
               Record that claim's (n_con, n_ent, S_size=|S|). RATIONALE: the item DEMOTEs in E37 iff SOME claim
               set-vetoes (con_share>=P AND n_ent==0) -> the max-con_share claim is the threshold driver; D_0 on it
               reproduces the item veto. CLASS-BLIND (true_class never read in selection).
- EMPTY-S = ACCEPT (v3 fix): a non-pre_demoted item whose auth claims all fall below B_floor (S_size==0), OR which
               has no auth resolution at all (pure PASS-COMMON), is VERIFIED/PASS in E37 -> ACCEPTED by construction
               (no bound set -> nothing to veto). representative returns (0,0,0). This is REPRODUCED, not VOIDed.
               The universal correctness gate is sec6 (emit decision == E37 op label), NOT a shape assertion.

## sec3 STATISTIC + DECISION RULE
D_lambda(record) = (n_con - lambda * n_ent) / S_size              [class-blind; reads only observable counts]
  reject(record, lambda, tau) = pre_demoted OR (NOT pre_demoted AND S_size>0 AND D_lambda >= tau)
  accept = otherwise.
  D is COMPUTED only when (not pre_demoted) and S_size>0 ; SKIPPED for pre_demoted (-> reject) and for
  non-pre_demoted empty-S (-> ACCEPT, VERIFIED). No fabricated con anywhere (R7).
  fa_op = spoof_accepted / spoof_total ; B2 = gold_accepted / gold_total. (max-based op gate; NOT p50 sep.)
  lambda=0 => D_0 = n_con/S_size = con_share (STRUCTURAL identity, exact float, asserted per row).

## sec4 REGRESSION ANCHOR (HARD ; lambda=0 @ tau_E37 ; one rung in the K=+inf/A=+inf/W=0/P=0 discipline)
(1) STRUCTURAL : for every non-pre_demoted row, D_0 == con_share exactly (formula identity). Drift => VOID.
(2) VERDICT    : lambda=0, tau = tau_E37 = 0.67 over ALL 30 spoofs:
                   16 pre_demoted (noauth) + 13 con-veto (D_0 >= 0.67) + 1 survive => 29 reject / 30
                   => fa_op = 1/30 = 0.0333 (4dp). round(fa_op,4) != 0.0333 => VOID (composite-anchor miss).
(3) DIAGNOSTIC : auth-only (exclude the 16 pre_demoted) = 14 spoofs ; con-veto reject 13/14 => fa = 0.0714.
                   This is the model-independent decomposition proof (NOT the gate anchor). 0.0714 != 0.0714 => VOID.
RATIONALE (carried, do not regress): 0.0333 is a con-veto(13)+noauth(16) COMPOSITE. A pure con/ent statistic
CANNOT reproduce it without the explicit pre_demoted channel (auth-only D = 0.0714 != 0.0333). The split is the fix.

## sec5 GRIDS + FORK (FROZEN ; byte-identical from run_E39_probe.py v1 -- kept, NOT re-chosen)
LAMBDA_GRID = [0.0, 0.25, 0.5, 1.0, 2.0]   (0.0 mandatory = degenerate -> con_share anchor)
TAU_GRID    = [0.20, 0.25, 0.30, 0.3333, 0.40, 0.50, 0.60] ; effective tau_set = TAU_GRID UNION {tau_E37=0.67}
FA_GATE = 0.10 ; B2_GATE = 0.90.
OP-SELECTION (oracle-clean): peek ONLY B2 >= B2_GATE (recall feasibility), then fa <= FA_GATE, then lambda > 0.0
  (lift requires the NEW axis -- pre_demoted rows reject at every lambda and cannot create the lift).
FORK (sec6 verdict):
  band = { op-points with B2>=0.90 AND fa<=0.10 AND lambda>0 }
  STATISTIC_LIFTS        : >= 2 distinct op-points across >= 2 distinct lambda (not a single C-point; cf E27/E35).
  STATISTIC_KNIFE_EDGE   : band non-empty but trivial (single point / single lambda).
  CROSS_SOURCE_EXHAUSTED : band empty -> net-consensus D does NOT clear the auth-subset gate -> FALSIFIER.
best_op = min fa, then max B2, over the band.

## sec6 RECONCILE SELF-CHECK (emitter MUST pass BEFORE writing the json -- the real pairing gate)
SPOOF-ONLY per-item reconcile. For every SPOOF item the emit decision MUST equal E37's own op-point label:
  emit_reject(item) := pre_demoted OR (representative con_share >= 0.67)
  e37_demote(item)  := item_label_at_C(res, C, B_floor=0.1, scope="any", A=0.35, W=0.0, P=0.67) == "DEMOTE"
  ASSERT emit_reject == e37_demote for ALL SPOOFS. Mismatch => VOID (spoof representative unfaithful -- e.g. a spoof
  entailer-rescued at con_share>=0.67 would also break the sec4 fa anchor).
GOLDS ARE EXCLUDED from this per-item check BY DESIGN: D_lambda is built to DIVERGE from con_share via n_ent. An
entailer-rescued gold (con_share>=P AND n_ent>=1) is VERIFIED in E37 but rejected by D_0 at lambda=0; that gap is
exactly what the n_ent term closes for lambda>0 -- the LIFT, measured via B2 in the sweep (sec5), not anchored here.
The lambda=0 anchor reproduces the E37 SPOOF fa only; gold B2 at lambda=0 is expected LOW and is not a reconcile target.
Then ASSERT the sec4 anchor (29/30 -> 0.0333 ; auth-only 13/14 -> 0.0714) on the emitted records.

## sec7 FALSIFIERS (state -> VOID, do not proceed)
- anchor (2) miss: lambda=0,tau_E37 != 0.0333 at 4dp over 30. (composite anchor not reproduced.)
- any pre_demoted row carrying a fabricated/non-zero invented con_share (R7).
- auth-only diagnostic != 0.0714 (1/14).
- sec6 self-check mismatch on any item (representative != E37 op label).
- heldout/fixture md5 mismatch (sec0).

## sec8 OUTPUTS (this rung)
emit_E37_readout.py -> eval/_local/E37_boundset.json (6-field; gitignored; OVERWRITES the retrieval map).
run_E39_probe.py v2 -> reports/report_E39.md + reports/gate_E39.log + reports/E39_Dnet.json.
COMMIT (onto-research/main, EXPLICIT add): reports/E39_RECONCILE_DEFECT.md run_E39_probe.py emit_E37_readout.py
  reports/PRE_REGISTER_E39.md reports/report_E39.md reports/gate_E39.log reports/E39_Dnet.json. single -m. push.
  E37_boundset.json + heldout + fixture stay eval/_local (gitignored; NEVER public).
